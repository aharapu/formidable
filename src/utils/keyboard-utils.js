export function isCharacterKey(key) {
    return key.length === 1;
}

export function isEnterKey(key) {
    return key === 'Enter';
}

export function isEscapeKey(key) {
    return key === 'Escape';
}
